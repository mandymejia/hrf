% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrub.R
\name{apply_scrubbing_single}
\alias{apply_scrubbing_single}
\title{Apply scrubbing by adding spike regressors to nuisance matrix}
\usage{
apply_scrubbing_single(nuisance = NULL, scrub = NULL, nT, verbose = 1)
}
\arguments{
\item{nuisance}{Nuisance matrix (timepoints x nuisance_regressors) or NULL.}

\item{scrub}{(Optional) A \eqn{T \times N_{scrub}} matrix of spike regressors
(one 1 value at the timepoint to scrub, and 0 for all other values), or a
logical vector indicating the timepoints to scrub (\code{TRUE} to scrub, and
\code{FALSE} to keep).

The spike regressors will be included in the nuisance
regression, and afterwards the timepoints indicated in \code{scrub} will be
removed from the BOLD data and design matrix.}

\item{nT}{The number of timepoints in the dataset. Used to validate that
scrub vectors, spike regressors, and nuisance regressors have consistent
dimensions.}

\item{verbose}{\code{1} (default) to print occasional updates during model
computation; \code{2} for occasional updates as well as running INLA in
verbose mode (if \code{Bayes}), or \code{0} for no printed updates.}
}
\value{
Updated nuisance matrix with spike regressors appended.
}
\description{
This function handles scrubbing (removal of problematic timepoints) by adding
spike regressors to the nuisance matrix. The actual "removal" happens during
nuisance regression - we don't physically remove timepoints from data.
}
\details{
Supports two input formats:
\enumerate{
\item Logical vector: TRUE/FALSE indicating which timepoints to scrub
\item Spike regressor matrix: Pre-formatted matrix with one column per scrubbed timepoint
}
}
\examples{
# Example 1: Logical vector (scrub timepoints 5, 10, 15)
nT <- 100
scrub_logical <- rep(FALSE, nT)
scrub_logical[c(5, 10, 15)] <- TRUE

# Example 2: Spike regressor matrix (3 columns for 3 scrubbed timepoints)
scrub_spikes <- matrix(0, nrow = nT, ncol = 3)
scrub_spikes[5, 1] <- 1   # First spike at timepoint 5
scrub_spikes[10, 2] <- 1  # Second spike at timepoint 10
scrub_spikes[15, 3] <- 1  # Third spike at timepoint 15
}
\keyword{internal}
