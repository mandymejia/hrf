% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_regularize_allHRFs.R
\name{plot_pop_avg_continuous}
\alias{plot_pop_avg_continuous}
\title{Plot continuous (imputed, non-snapped) per-voxel HRF parameter}
\usage{
plot_pop_avg_continuous(
  x,
  param = c("a1", "b1"),
  fname = NULL,
  title = NULL,
  shadows = 1,
  material = list(lit = TRUE, smooth = FALSE),
  NA_color = "#505560",
  zlim = NULL,
  surf_FWHM = 4,
  impute_method = "median",
  ...
)
}
\arguments{
\item{x}{A \code{regularizeHRFs} object.}

\item{param}{One of \code{"a1"} or \code{"b1"}.}

\item{fname, title, shadows, material, NA_color, zlim}{Plot args (see
\code{plot_pop_avg}).}

\item{surf_FWHM}{Surface smoothing FWHM (mm) for the imputation step.
Default \code{4} matches \code{unmask_pop_avg}.}

\item{impute_method}{Passed to \code{unmask_xifti}. Default \code{"median"}.}

\item{...}{Additional args passed to \code{plot.xifti}.}
}
\value{
Invisibly returns the plot result.
}
\description{
Computes the per-voxel mean of \code{best_params_df$<param>} across
subjects (mask-filtered), sprays it onto a full cortical xifti, then
fills + smooths it with \code{unmask_xifti} (same imputation used inside
\code{regularize_allHRFs} for t2p/FWHM). Plots the resulting continuous
map. Unlike \code{type = "pop_avg"} (which snaps to the HRF grid), this
preserves the continuous between-grid values.
}
\keyword{internal}
