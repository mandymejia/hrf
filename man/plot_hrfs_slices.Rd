% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hrf_grid.R
\name{plot_hrfs_slices}
\alias{plot_hrfs_slices}
\title{Plot tapered HRFs from parameter grid (internal)}
\usage{
plot_hrfs_slices(
  hrf_grid,
  canonical_a1 = 6,
  canonical_b1 = 1,
  c_val = 1/6,
  dashed_a1 = c(3, 5, 7, 9, 11),
  dashed_b1 = c(0.75, 1.25, 1.75)
)
}
\arguments{
\item{hrf_grid}{Data frame with HRF parameters from \code{generate_hrf_grid}.}

\item{canonical_a1, canonical_b1}{The fixed canonical parameter values.
Defaults: SPM canonical (\code{a1 = 6}, \code{b1 = 1}).}

\item{c_val}{Single c value to use. If \code{hrf_grid} has multiple c values,
the grid is filtered to this one. Default \code{1/6}.}

\item{dashed_a1, dashed_b1}{Values to draw as dashed (alternating to help
distinguish neighboring swatches in the discrete palette).}
}
\value{
A \code{grid.arrange} grob with two stacked panels.
}
\description{
Two-panel "slices" view of the HRF parameter grid (internal)
}
\details{
Vary a1 with b1 fixed at canonical, then vary b1 with a1 fixed at canonical.
Canonical HRF drawn thick + black in both panels. Discrete diverging
(ROY_BIG_BL) palette centered at the canonical value.
}
\keyword{internal}
