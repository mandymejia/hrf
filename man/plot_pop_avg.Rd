% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_regularize_allHRFs.R
\name{plot_pop_avg}
\alias{plot_pop_avg}
\title{Plot Population-Average HRF Parameter Map (Brain Surface)}
\usage{
plot_pop_avg(
  x,
  param = c("a1", "b1", "c", "time_to_peak", "FWHM"),
  fname = NULL,
  title = NULL,
  shadows = 1,
  material = list(lit = TRUE, smooth = FALSE),
  NA_color = "#505560",
  zlim = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{regularizeHRFs} object.}

\item{param}{One of \code{"a1"}, \code{"b1"}, \code{"c"}.}

\item{fname}{Optional path to save the rendered PNG (or HTML on Mac without
working OpenGL).}

\item{title}{Optional plot title. Defaults to \code{"Population avg <param> (snapped)"}.}

\item{shadows, material, NA_color}{Standard \code{plot.xifti} aesthetics.}

\item{zlim}{Optional length-2 numeric. If \code{NULL}, dynamically derived
from \code{range(x$hrf_grid[[param]])} so the colorbar always spans the
actual grid the user picked.}

\item{...}{Additional args passed to \code{plot.xifti}.}
}
\value{
Invisibly returns the plot result.
}
\description{
Renders the population-averaged HRF parameter from \code{x$pop_avg}
(snapped to the HRF grid) onto the cortex-surface xifti template attached
as \code{attr(x, "xii")} by \code{regularize_allHRFs()}.
}
\keyword{internal}
