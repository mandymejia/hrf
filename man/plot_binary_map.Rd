% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fit_workingHRF.R
\name{plot_binary_map}
\alias{plot_binary_map}
\title{Plot binary activation mask across subjects}
\usage{
plot_binary_map(
  x,
  threshold = 0.1,
  alpha = NULL,
  colors = "viridis",
  title = NULL,
  fname = NULL,
  width = 1200,
  height = 800,
  shadows = 1,
  material = list(lit = TRUE, smooth = FALSE),
  ...
)
}
\arguments{
\item{x}{A \code{workingHRF} object.}

\item{threshold}{Numeric. Proportion threshold for declaring activation (default: \code{0.1}).}

\item{alpha}{Optional numeric. Significance threshold for F-test p-values (e.g., \code{0.001}).
If not provided, uses the \code{alpha} stored in \code{x$activation_masks}.}

\item{colors}{Color map for the surface (default: \code{"viridis"}).}

\item{title}{Optional plot title. Auto-generated if not provided.}

\item{fname}{Optional filename to save the plot (e.g., \code{"plot.png"}).}

\item{width, height}{Pixel dimensions for saved plot (default: \code{1200x800}).}

\item{shadows}{Shadow depth for surface rendering (default: \code{1}).}

\item{material}{List of lighting/material properties for rendering
(e.g., \code{list(lit = TRUE, smooth = FALSE)}).}

\item{...}{Additional arguments passed to \code{\link[ciftiTools]{view_xifti_surface}}.}
}
\value{
Invisibly returns a \code{xifti} object containing the binary activation mask.
The surface plot is rendered or saved as specified.
}
\description{
Displays a surface plot of the binary activation mask based on a user-specified
proportion threshold and significance level. Each brain location is marked as active (1)
if the proportion of subjects with F-test p-values below \code{alpha} exceeds \code{threshold}.
}
\details{
The surface is rendered using the geometry from the first successful subject.
}
\keyword{internal}
