% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fit_allHRFs.R
\name{plot_hrf_preview}
\alias{plot_hrf_preview}
\title{Preview HRF Design Matrix from Parameter Grid}
\usage{
plot_hrf_preview(
  hrf_grid,
  EVs,
  nT,
  TR,
  hrf_idx = 1,
  onsets = TRUE,
  offsets = TRUE,
  ...
)
}
\arguments{
\item{hrf_grid}{A data frame or list with HRF parameter combinations. Must contain
columns or list elements named \code{a1}, \code{a2}, \code{b1}, \code{b2}, and \code{c}.}

\item{EVs}{List, same length as \code{BOLD}. Each element is a data frame
(or similar) giving event onsets and durations for that subject. Each
data frame should have columns for onset times and durations in seconds.}

\item{nT}{The number of timepoints in the dataset. Used to validate that
scrub vectors, spike regressors, and nuisance regressors have consistent
dimensions.}

\item{TR}{Repetition time (TR) in seconds.}

\item{hrf_idx}{Integer index into the HRF grid specifying which HRF to preview.}

\item{onsets}{Logical or character vector. Add separate onset regressors
per trial type? If \code{TRUE}, adds onset regressors for all tasks.
If character vector, specifies which tasks to model onsets for.
Only compatible with block designs (events with duration > 1s).}

\item{offsets}{Logical or character vector. Add separate offset regressors
per trial type? If \code{TRUE}, adds offset regressors for all tasks.
If character vector, specifies which tasks to model offsets for.
Only compatible with block designs (events with duration > 1s).}

\item{...}{Passed to \code{\link[ggplot2]{theme}}, \code{\link[ggplot2]{facet_grid}},
or other internal ggplot functions for additional customization.}
}
\value{
A named list of \code{ggplot2} objects: \code{main}, \code{dhrf}, and \code{ddhrf}
(if applicable). Also prints the plots.
}
\description{
This function previews the design matrix for a single HRF configuration from a grid,
including optional onset/offset modeling. It is primarily used for inspecting how the
modeled regressors vary with different HRF parameters.
}
