% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regularize_allHRFs.R
\name{slope_fun_WLS}
\alias{slope_fun_WLS}
\title{Compute Subject-Level Slope Using Weighted Least Squares}
\usage{
slope_fun_WLS(gamma_iv, gamma_v, weight_A, lm = FALSE)
}
\arguments{
\item{gamma_iv}{Numeric vector of subject-specific HRF parameter estimates
(length = number of vertices used for a specific subject).}

\item{gamma_v}{Numeric vector of voxel-wise population-averaged HRF parameters.}

\item{weight_A}{Numeric vector of weights (typically inverse variance) for
weighted least squares estimation.}

\item{lm}{Logical; currently unused, kept for compatibility. The function
uses manual weighted calculation rather than \code{lm()}.}
}
\value{
Numeric scalar representing the weighted slope estimate, or NA if
weight normalization fails.
}
\description{
Computes the weighted slope of subject-level parameter estimates (\code{gamma_iv})
against population-averaged estimates (\code{gamma_v}) using inverse variance
weighting for improved estimation accuracy.
}
\details{
The function performs weighted least squares slope estimation by:
\enumerate{
\item Computing weighted means for centering both variables
\item Normalizing weights to sum to 1 for numerical stability
\item Computing the weighted slope using the manual formula
}

The manual approach is used instead of \code{lm()} to maintain control over
the weight normalization and reuse normalized weights efficiently.
}
\keyword{internal}
