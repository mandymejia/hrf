% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_regularize_allHRFs.R
\name{plot_precision}
\alias{plot_precision}
\title{Plot Precision (Inverse Variance) Maps}
\usage{
plot_precision(
  x,
  param,
  model = c("IS", "IO"),
  fname,
  title = NULL,
  legend_fname = NULL,
  shadows = 1,
  material = list(lit = TRUE, smooth = FALSE),
  NA_color = "#505560",
  ...
)
}
\arguments{
\item{x}{A \code{regularizeHRFs} object from \code{\link{regularize_allHRFs}}.}

\item{param}{Character string specifying which HRF parameter to plot
(\code{"a1"}, \code{"b1"}, or \code{"c"}).}

\item{model}{Character string specifying which regularization fit to visualize:
\code{"IS"} for the intercept-slope fit, or \code{"IO"} for the intercept-only fit.}

\item{fname}{Optional file path to save the plot. If \code{NULL}, the plot
is displayed interactively but not saved.}

\item{title}{Optional custom plot title. If \code{NULL}, a descriptive title
is generated automatically.}

\item{legend_fname}{Optional file path to save the color legend separately.}

\item{shadows}{Numeric value controlling shadow intensity (default = 1).}

\item{material}{List specifying surface rendering properties (default:
\code{list(lit = TRUE, smooth = FALSE)}).}

\item{NA_color}{Color to use for NA or masked values (default = "#505560").}

\item{...}{Additional arguments passed to the underlying plotting function.}
}
\value{
Invisibly returns the plot object.
}
\description{
Creates brain surface plots showing the \emph{precision} (inverse residual variance)
derived from OLS or WLS regression models used in HRF regularization.
These maps highlight areas of higher certainty or reliability in the estimated
HRF parameters across the cortical surface.
}
\details{
Precision maps are computed as:
\deqn{Precision = 1 / Variance}

Higher precision values indicate more stable and reliable parameter estimates
across subjects. The function automatically aligns color scales across the two
regularization fits (\code{"IS"} and \code{"IO"}) to allow direct visual comparison.
}
\keyword{internal}
