% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regularize_allHRFs.R
\name{run_parallel_subjects_refit}
\alias{run_parallel_subjects_refit}
\title{Process candidate subjects in parallel using cluster workers}
\usage{
run_parallel_subjects_refit(
  candidate_maps,
  hrf_grid,
  session_data,
  TR,
  brainstructures,
  hpf,
  onsets,
  offsets,
  unique_grid,
  idx_map,
  pop_voxels,
  n_candidates,
  verbose,
  n_cores,
  log_dir
)
}
\arguments{
\item{candidate_maps}{List of candidate map data.frames from
\code{create_candidate_maps()}.}

\item{hrf_grid}{HRF grid with a1, b1, c, a2, b2 columns.}

\item{session_data}{List with BOLD_files, EVs_list, nuisance_files.}

\item{TR}{Numeric. Repetition time in seconds.}

\item{brainstructures}{Character vector. Brain structures to model.}

\item{hpf}{Numeric. High-pass filter cutoff.}

\item{onsets}{Logical. Include onset regressors.}

\item{offsets}{Logical. Include offset regressors.}

\item{unique_grid}{Compact HRF grid restricted to model indices referenced by
candidate maps.}

\item{idx_map}{Named integer vector mapping global model_idx (character key)
to compact unique_grid row index.}

\item{pop_voxels}{Integer vector of population-mask voxels (rows of RSS that
contribute to the weighted RSS sum).}

\item{n_candidates}{Integer. Number of candidate maps.}

\item{verbose}{Integer verbosity level.}

\item{n_cores}{Integer. Number of parallel worker processes (1 = sequential).}

\item{log_dir}{Directory for cluster log files (parallel mode only).}
}
\value{
List of single-row data.frames, one per subject.
}
\description{
Sets up a parallel cluster and processes all subjects simultaneously using
load-balanced \code{parLapplyLB()}. Each worker calls
\code{process_candidate_subject_refit()} for one subject.
}
\keyword{internal}
