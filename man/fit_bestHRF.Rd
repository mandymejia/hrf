% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bestHRF.R
\name{fit_bestHRF}
\alias{fit_bestHRF}
\title{Fit Best HRF GLM for a Single Subject}
\usage{
fit_bestHRF(
  regularize_result,
  BOLD_file,
  EVs,
  nuisance_file = NULL,
  TR,
  subject_idx = NULL,
  contrasts = NULL,
  working_hrf = list(a1 = 6, b1 = 1, c = 1/6),
  brainstructures = c("left", "right"),
  resamp_res = 10000,
  hpf = 0.01,
  onsets = FALSE,
  offsets = FALSE,
  p_adjust_method = "BH",
  verbose = 1
)
}
\arguments{
\item{regularize_result}{Output from \code{regularize_allHRFs()}.}

\item{BOLD_file}{Character. Path to subject's CIFTI file.}

\item{EVs}{Event data for this subject.}

\item{nuisance_file}{Character. Path to nuisance regressor file (or NULL).}

\item{TR}{Numeric. Repetition time in seconds.}

\item{subject_idx}{Integer. Subject index (required when seffects=TRUE).}

\item{contrasts}{Numeric matrix. Contrast matrix A (n_contrasts x n_task).
Default NULL uses identity (each beta tested individually).}

\item{brainstructures}{Character vector. Brain structures to model.}

\item{resamp_res}{Integer. Resampling resolution.}

\item{hpf}{Numeric. High-pass filter cutoff.}

\item{onsets, offsets}{Logical. Include onset/offset regressors.}

\item{p_adjust_method}{Character. P-value adjustment method.}

\item{verbose}{Integer. Verbosity level.}
}
\value{
A list with class \code{"bestHRF"} containing:
\describe{
\item{working}{Canonical HRF fit: betas and contrasts (as xifti)}
\item{population or adapted}{Regularized HRF fit: betas, contrasts,
hrf_assignments (voxel-to-HRF mapping), and subject_idx (if adapted)}
\item{df}{Degrees of freedom}
\item{contrast_matrix}{The contrast matrix A used}
}
}
\description{
Fits a voxel-wise GLM using personalized HRFs from regularize_allHRFs.
Each voxel gets its own design matrix based on the population or
subject-specific HRF map. Voxels sharing the same HRF are grouped
for efficiency.
}
